# Python authoring package

The current `src/cairn_plot` distribution moves here after the generated
`PlotSpec` schema and compatibility reader are established. Keeping the Hatch
package path unchanged during the first slices preserves release behavior.
