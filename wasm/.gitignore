# The Rust exr-decode crate was RETIRED (replaced by the OpenEXR Emscripten wasm
# under wasm/openexr/, which reads every EXR). Its build outputs are gone; see
# wasm/openexr/.gitignore for the OpenEXR build's ignored trees.
