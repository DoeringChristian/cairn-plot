# Rust build artifacts (regenerated by cargo / wasm-pack; not committed).
exr-decode/target/
# wasm-pack output — the committed, consumer-ready artifact is inline/ instead.
# (pkg/ also self-ignores via pkg/.gitignore, kept here for clarity.)
exr-decode/pkg/
